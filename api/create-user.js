import { PrismaClient } from '@prisma/client'; import bcrypt from 'bcryptjs'; import dotenv from 'dotenv'; dotenv.config(); const prisma = new PrismaClient(); const main = async () => { const hashedPassword = await bcrypt.hash('admin123', 12); const user = await prisma.user.create({ data: { email: 'grinder_kirov@mailnesia.com', name: 'Admin', role: 'ADMIN', passwordHash: hashedPassword } }); console.log('User created:', user); }; main().catch(console.error).finally(() => prisma.$disconnect());
